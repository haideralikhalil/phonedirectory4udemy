name: phonedirectory
description: Phone Directory App
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
  #sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  gsheets: ^0.4.2
  firebase_core: ^2.6.1
  cloud_firestore: ^4.4.2
  #firebase_core: ^1.8.0
  #cloud_firestore: ^2.5.4
  url_launcher:
  flutter_sms:
  csv: ^5.0.0
  cupertino_icons: ^1.0.2
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - res/icons/whatsapp.png
    - res/contacts.csv
